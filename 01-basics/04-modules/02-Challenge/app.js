const { add, sub, mult, divid } = require("./math");

console.log(`Addition ${add(2, 6)}`);
console.log(`Subtraction ${sub(8, 4)}`);
console.log(`Multiplication ${mult(4, 9)}`);
console.log(`Division ${divid(8, 3)}`);
